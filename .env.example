# Local Cloudflare Worker development only. Never commit real values.
DOTS_API_KEY=replace_with_your_dots_api_key
RATE_LIMIT_SALT=replace_with_a_long_random_value

# Optional build-time override. GA only loads after the visitor opts in.
VITE_GA_MEASUREMENT_ID=G-D23SKTMEZS
